//Default--Access only in same package

class DefaultAccessModifier {
    public static void main(String[] args) {
        DefaultAccessModifier d = new DefaultAccessModifier();
        //some content that be import from same package...
    }
}
